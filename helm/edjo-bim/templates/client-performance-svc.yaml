{{- if .Values.clientPerformanceSvc.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-performance-svc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "edjo-bim.labels" . | nindent 4 }}
    component: client-performance-svc
spec:
  replicas: {{ .Values.clientPerformanceSvc.replicas }}
  selector:
    matchLabels:
      {{- include "edjo-bim.selectorLabels" . | nindent 6 }}
      component: client-performance-svc
  template:
    metadata:
      labels:
        {{- include "edjo-bim.selectorLabels" . | nindent 8 }}
        component: client-performance-svc
    spec:
      containers:
      - name: client-performance-svc
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.clientPerformanceSvc.image }}:{{ .Values.global.imageTag }}"
        ports:
        - containerPort: {{ .Values.clientPerformanceSvc.port }}
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
        resources:
          {{- toYaml .Values.clientPerformanceSvc.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: {{ .Values.clientPerformanceSvc.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: {{ .Values.clientPerformanceSvc.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: client-performance-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    {{- include "edjo-bim.selectorLabels" . | nindent 4 }}
    component: client-performance-svc
  ports:
  - port: {{ .Values.clientPerformanceSvc.port }}
    targetPort: {{ .Values.clientPerformanceSvc.port }}
{{- end }}
